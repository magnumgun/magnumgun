<resources>

    <!-- Основная тема приложения -->
    <style name="Theme.UberClone" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>
    </style>

    <!-- Стиль для кнопки с закруглёнными углами и тенью -->
    <style name="CustomButtonStyle">
        <item name="android:background">@drawable/rounded_button</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:padding">10dp</item>
        <item name="android:elevation">8dp</item> <!-- Добавляет тень -->
    </style>

    <!-- Стиль для текстовых полей с закруглёнными углами -->
    <style name="CustomEditTextStyle">
        <item name="android:background">@drawable/rounded_edittext</item>
        <item name="android:padding">10dp</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textColorHint">#AAAAAA</item>
    </style>

</resources>
