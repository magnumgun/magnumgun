<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Разрешения для работы с интернетом и местоположением -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UberClone">

        <!-- Указываем LoginActivity как стартовую активность -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Регистрация RegistrationActivity -->
        <activity
            android:name=".RegistrationActivity"
            android:exported="true">
        </activity>

        <!-- Регистрация MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
        </activity>

        <!-- Активность с картой OpenStreetMap -->
        <activity
            android:name=".MapActivity"
            android:exported="true">
        </activity>

    </application>

</manifest>
